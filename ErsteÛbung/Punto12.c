#include <stdio.h>

int main(void) {
  long double tarjeta;
  char nombre[] = "Hola";
  double contraseña; 
  printf("ingrese su nombre,tarjeta de credito y contraseña separados por un espacio");
  scanf("%Lf%s", &tarjeta, &nombre[], &contraseña);
  scanf("%lf",&contraseña); 

  printf("Su nombre: %Lf\n ")

  return 0;
 //sizeof imprime el tamaño en bytes de las variables. 

}